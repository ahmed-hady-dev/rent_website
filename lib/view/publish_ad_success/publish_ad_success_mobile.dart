import 'package:flutter/material.dart';

class PublishAdSuccessMobile extends StatelessWidget {
  const PublishAdSuccessMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
