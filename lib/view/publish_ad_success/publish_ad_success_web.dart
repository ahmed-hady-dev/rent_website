import 'package:flutter/material.dart';

class PublishAdSuccessWeb extends StatelessWidget {
  const PublishAdSuccessWeb({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
